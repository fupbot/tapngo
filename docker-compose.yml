services:
  backend:
    build: ./backend
    environment:
      # Persistent volume below, not the image's own filesystem — a
      # container recreation must not wipe stock levels / order history.
      DATABASE_URL: sqlite:////app/data/tapngo.db
    volumes:
      - backend_data:/app/data
    expose:
      - "8000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  backend_data:
