# Identifies this browser tab as a specific physical kiosk terminal.
# Copy to .env.local and change per deployed tablet — not a login, just a
# per-terminal label so orders can be traced back to which kiosk placed them.
VITE_KIOSK_ID=kiosk1
